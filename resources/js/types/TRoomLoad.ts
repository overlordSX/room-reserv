export type TRoomLoad = {
    id: number,
    name?: string,
    phone?: string,
    people?: number,
    bgColor?: string,
    startDate: string,
    endDate: string,
    cellDate?: string,
    isApproved: boolean,
}
